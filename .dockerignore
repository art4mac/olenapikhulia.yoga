# Keep the build context to the deployable site — the raw shoot is ~1.3 GB and
# would otherwise be shipped to the daemon on every build.
Photos/
Selected Photos/

.git/
.gitignore
.claude/
.remember/
.DS_Store
docker-compose.yml
README.md
